---
phase: adhoc-no-preinstall
task: 3
total_tasks: 8
status: in_progress
last_updated: 2026-03-31T07:09:19.171Z
---

<current_state>
no-preinstall 빌드 variant 구현 중. Rust 백엔드 절반 완료, 프론트엔드/CI/README 미착수.
uncommitted 변경: src-tauri/src/download.rs, src-tauri/src/deps.rs
</current_state>

<completed_work>

- Task 1: `locate_sidecar` 확장 — done
  - 탐색 순서: env var(YTDLP_PATH/FFMPEG_PATH) → 설정 저장 커스텀 경로 → 번들 사이드카 → 시스템 PATH
  - macOS Homebrew 경로(/opt/homebrew/bin, /usr/local/bin) 하드코딩 추가 (앱번들에서 PATH 짧음)
  - `read_dep_path_from_settings(key)` — app-settings.json에서 직접 읽음
  - `which_in_path(name)` — PATH 탐색 헬퍼

- Task 2: `deps.rs` 생성 — done
  - `check_deps()` → `{ ytdlp: DepStatus, ffmpeg: DepStatus }` 반환 (found, path, version)
  - `set_dep_path(key, path)` → app-settings.json에 dep_path_ytdlp / dep_path_ffmpeg 저장
  - `probe_dep` / `get_version` 내부 헬퍼

</completed_work>

<remaining_work>

- Task 3: Cargo.toml에 `dirs-next = "0.3"` 추가 (deps.rs가 dirs_next::config_dir() 사용)
- Task 4: lib.rs 수정
  - `mod deps;` 추가
  - invoke_handler에 `deps::check_deps`, `deps::set_dep_path` 추가
  - `cargo build` 로 컴파일 확인
- Task 5: `src-tauri/tauri.nopreinstall.conf.json` 생성
  - tauri.conf.json 복사 후 `bundle.externalBin` 배열 제거
  - productName을 "mixtape-nopreinstall" 등으로 구분할 필요 없음 (같은 앱)
- Task 6: 프론트엔드
  - App.tsx: 시작 시 `check_deps` 호출 → 하나라도 없으면 `depsReady=false` 상태
  - `SetupScreen` 컴포넌트: yt-dlp/ffmpeg 각각 상태 표시, 설치 링크, 파일피커로 수동 경로 지정
  - `depsReady` false이면 메인 UI 대신 SetupScreen 렌더링
  - Settings 탭에 "DEPENDENCIES" 섹션 추가: 각 dep 경로/버전 표시 + 파일피커 변경 버튼
- Task 7: CI — `.github/workflows/release.yml` 에 no-preinstall job 3개 추가
  - build-macos-nopreinstall: 사이드카 다운로드 없음, `--config src-tauri/tauri.nopreinstall.conf.json`
  - build-windows-nopreinstall: 동일
  - build-linux-nopreinstall: 동일
  - 릴리즈 아티팩트 이름에 `-nopreinstall` 또는 `-np` suffix
- Task 8: README 업데이트
  - Download 섹션에 두 버전 표 추가
  - Full: 사이드카 포함, 설치 후 바로 사용
  - No-Preinstall: yt-dlp + ffmpeg 별도 설치 필요, 설치 방법 링크

</remaining_work>

<decisions_made>

- no-preinstall 버전은 앱이 직접 다운로드하지 않음 — 요구사항으로만 안내 (사용자 결정)
- locate_sidecar가 시스템 PATH + Homebrew 경로 탐색 — brew install 한 사람 자동 감지
- 커스텀 경로는 app-settings.json에 dep_path_ytdlp / dep_path_ffmpeg 키로 저장
- tauri.nopreinstall.conf.json으로 빌드 variant 분리 (Rust 코드 공유)
- CI에서 `tauri-action`의 `--config` 플래그로 variant 선택

</decisions_made>

<blockers>
없음
</blockers>

<context>
이 세션에서 완료한 것들:
- Phase 5 (test-infrastructure) 실행 + 검증 완료
- v0.1.9 릴리즈 (search error surfacing, si param strip, music.youtube.com URL 지원)
- 쿠키 이중 프롬프트 수정 (자동감지 제거, 사용자 설정 브라우저만 사용)
- 빌드 경량화: LTO, strip, rustls, tauri-plugin-shell 제거
- no-preinstall variant 구현 시작 (Rust 백엔드 절반)

Rust locate_sidecar 구조는 양쪽 빌드에서 동일하게 동작함.
full 빌드는 사이드카를 먼저 찾고, no-preinstall 빌드는 사이드카 없으니 PATH로 fallthrough.
</context>

<next_action>
Start with: Cargo.toml에 `dirs-next = "0.3"` 추가 → `cargo build` 확인 → lib.rs에 `mod deps` + invoke_handler 등록 → tauri.nopreinstall.conf.json 생성 → 프론트엔드 SetupScreen
</next_action>
