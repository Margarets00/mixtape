---
phase: 05-test-infrastructure
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - .github/workflows/release.yml
autonomous: true
requirements: [D-10, D-11, D-12, D-13, D-14]

must_haves:
  truths:
    - "CI runs cargo test before building on all three platforms"
    - "CI verifies sidecar filenames exist after download-sidecars script and before tauri-action build"
    - "CI trigger remains v* tag push only"
  artifacts:
    - path: ".github/workflows/release.yml"
      provides: "cargo test step + sidecar filename verification step in all 3 platform jobs"
      contains: "cargo test"
  key_links:
    - from: ".github/workflows/release.yml (cargo test step)"
      to: "src-tauri/src/download.rs (tests)"
      via: "cargo test --manifest-path src-tauri/Cargo.toml"
      pattern: "cargo test"
    - from: ".github/workflows/release.yml (sidecar check step)"
      to: "src-tauri/binaries/"
      via: "test -f / Test-Path file existence check"
      pattern: "Verify sidecar files exist"
---

<objective>
Add `cargo test` and sidecar filename verification steps to the GitHub Actions release workflow for all three platforms (macOS, Windows, Linux).

Purpose: Catch test failures and missing sidecar binaries before the slow Tauri build step, providing fast CI feedback.
Output: Updated release.yml with two new steps per platform job inserted between sidecar download and tauri-action.
</objective>

<execution_context>
@/Users/margarets/Dev_margaret/youtube-downloader/.claude/get-shit-done/workflows/execute-plan.md
@/Users/margarets/Dev_margaret/youtube-downloader/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-test-infrastructure/05-CONTEXT.md
@.planning/phases/05-test-infrastructure/05-RESEARCH.md
@.planning/phases/05-test-infrastructure/05-01-SUMMARY.md

<interfaces>
<!-- Current release.yml structure (step order per job) -->

build-macos:
  1. actions/checkout@v4
  2. actions/setup-node@v4
  3. dtolnay/rust-toolchain@stable (targets: aarch64-apple-darwin,x86_64-apple-darwin)
  4. swatinem/rust-cache@v2
  5. npm ci
  6. Download sidecars (macOS universal) -- scripts/download-sidecars-ci.sh
  7. tauri-apps/tauri-action@v0  <-- INSERT cargo test + sidecar check BEFORE this

build-windows:
  1. actions/checkout@v4
  2. actions/setup-node@v4
  3. dtolnay/rust-toolchain@stable
  4. swatinem/rust-cache@v2
  5. npm ci
  6. Download sidecars (Windows) -- scripts/download-sidecars.sh (shell: bash)
  7. tauri-apps/tauri-action@v0  <-- INSERT cargo test + sidecar check BEFORE this

build-linux:
  1. actions/checkout@v4
  2. actions/setup-node@v4
  3. dtolnay/rust-toolchain@stable
  4. swatinem/rust-cache@v2
  5. Install Linux dependencies (apt-get)
  6. npm ci
  7. Download sidecars (Linux) -- scripts/download-sidecars.sh
  8. tauri-apps/tauri-action@v0  <-- INSERT cargo test + sidecar check BEFORE this

Expected sidecar filenames (from download-sidecars-ci.sh / download-sidecars.sh):
- macOS: yt-dlp-aarch64-apple-darwin, yt-dlp-x86_64-apple-darwin, ffmpeg-aarch64-apple-darwin, ffmpeg-x86_64-apple-darwin
- Windows: yt-dlp-x86_64-pc-windows-msvc.exe, ffmpeg-x86_64-pc-windows-msvc.exe
- Linux: yt-dlp-x86_64-unknown-linux-gnu, ffmpeg-x86_64-unknown-linux-gnu
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cargo test + sidecar verification steps to all platform jobs in release.yml</name>
  <files>.github/workflows/release.yml</files>
  <read_first>
    - .github/workflows/release.yml (full file — understand exact step order and indentation per job)
    - .planning/phases/05-test-infrastructure/05-RESEARCH.md (lines 186-230 — exact sidecar filenames and CI step templates per platform)
  </read_first>
  <action>
In `.github/workflows/release.yml`, insert two new steps in each of the three platform jobs. The steps go AFTER the "Download sidecars" step and BEFORE the `tauri-apps/tauri-action@v0` step.

**For build-macos** (insert after "Download sidecars (macOS universal)" step, before tauri-action):
```yaml
      - name: Run Rust unit tests
        run: cargo test --manifest-path src-tauri/Cargo.toml -- --test-threads=1

      - name: Verify sidecar files exist
        run: |
          test -f src-tauri/binaries/yt-dlp-aarch64-apple-darwin   || { echo "MISSING: yt-dlp-aarch64-apple-darwin"; exit 1; }
          test -f src-tauri/binaries/yt-dlp-x86_64-apple-darwin    || { echo "MISSING: yt-dlp-x86_64-apple-darwin"; exit 1; }
          test -f src-tauri/binaries/ffmpeg-aarch64-apple-darwin    || { echo "MISSING: ffmpeg-aarch64-apple-darwin"; exit 1; }
          test -f src-tauri/binaries/ffmpeg-x86_64-apple-darwin     || { echo "MISSING: ffmpeg-x86_64-apple-darwin"; exit 1; }
          echo "Sidecar check passed."
```

**For build-windows** (insert after "Download sidecars (Windows)" step, before tauri-action):
```yaml
      - name: Run Rust unit tests
        run: cargo test --manifest-path src-tauri/Cargo.toml -- --test-threads=1

      - name: Verify sidecar files exist
        shell: pwsh
        run: |
          $missing = @()
          if (-not (Test-Path "src-tauri/binaries/yt-dlp-x86_64-pc-windows-msvc.exe")) { $missing += "yt-dlp-x86_64-pc-windows-msvc.exe" }
          if (-not (Test-Path "src-tauri/binaries/ffmpeg-x86_64-pc-windows-msvc.exe")) { $missing += "ffmpeg-x86_64-pc-windows-msvc.exe" }
          if ($missing.Count -gt 0) { Write-Error "MISSING sidecars: $($missing -join ', ')"; exit 1 }
          Write-Host "Sidecar check passed."
```

**For build-linux** (insert after "Download sidecars (Linux)" step, before tauri-action):
```yaml
      - name: Run Rust unit tests
        run: cargo test --manifest-path src-tauri/Cargo.toml -- --test-threads=1

      - name: Verify sidecar files exist
        run: |
          test -f src-tauri/binaries/yt-dlp-x86_64-unknown-linux-gnu  || { echo "MISSING: yt-dlp-x86_64-unknown-linux-gnu"; exit 1; }
          test -f src-tauri/binaries/ffmpeg-x86_64-unknown-linux-gnu   || { echo "MISSING: ffmpeg-x86_64-unknown-linux-gnu"; exit 1; }
          echo "Sidecar check passed."
```

Key details:
- Windows sidecar check uses `shell: pwsh` and `Test-Path` (not bash `test -f`) per D-13 and research pitfall 5
- All cargo test steps use `--test-threads=1` to avoid env var race conditions per research recommendation
- The trigger (`on: push: tags: v*`) remains unchanged per D-14
- No separate workflow file per D-10
  </action>
  <verify>
    <automated>grep -c "Run Rust unit tests" .github/workflows/release.yml | grep -q "3" && grep -c "Verify sidecar files exist" .github/workflows/release.yml | grep -q "3" && echo "PASS: 3 cargo test + 3 sidecar check steps found" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'Run Rust unit tests' .github/workflows/release.yml returns 3
    - grep -c 'Verify sidecar files exist' .github/workflows/release.yml returns 3
    - grep -q 'cargo test --manifest-path src-tauri/Cargo.toml -- --test-threads=1' .github/workflows/release.yml
    - grep -q 'yt-dlp-aarch64-apple-darwin' .github/workflows/release.yml (macOS sidecar check)
    - grep -q 'yt-dlp-x86_64-pc-windows-msvc.exe' .github/workflows/release.yml (Windows sidecar check)
    - grep -q 'yt-dlp-x86_64-unknown-linux-gnu' .github/workflows/release.yml (Linux sidecar check)
    - grep -q 'shell: pwsh' .github/workflows/release.yml (Windows uses PowerShell for Test-Path)
    - grep -q "tags:" .github/workflows/release.yml (trigger unchanged)
    - The "Run Rust unit tests" step appears BEFORE "tauri-apps/tauri-action" in each job
    - The "Verify sidecar files exist" step appears AFTER "Download sidecars" in each job
  </acceptance_criteria>
  <done>release.yml has cargo test and sidecar filename verification in all 3 platform jobs, inserted between sidecar download and tauri build. Windows uses PowerShell. Trigger unchanged at v* tags.</done>
</task>

</tasks>

<verification>
Validate YAML syntax of the updated workflow:
```bash
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/release.yml'))" && echo "YAML valid" || echo "YAML invalid"
```
Verify step ordering:
```bash
grep -n "Download sidecars\|Run Rust unit tests\|Verify sidecar files exist\|tauri-apps/tauri-action" .github/workflows/release.yml
```
Expected ordering per job: Download sidecars < Run Rust unit tests < Verify sidecar files exist < tauri-action.
</verification>

<success_criteria>
- release.yml is valid YAML
- Each of the 3 platform jobs has: cargo test step + sidecar check step, both before tauri-action
- Windows sidecar check uses pwsh shell with Test-Path
- macOS/Linux sidecar checks use bash test -f
- CI trigger remains v* tag push only
</success_criteria>

<output>
After completion, create `.planning/phases/05-test-infrastructure/05-02-SUMMARY.md`
</output>
